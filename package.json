{
  "name": "node-hello",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "build:marketplace": "npm --prefix contractormpapp install && npm --prefix contractormpapp run build"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
